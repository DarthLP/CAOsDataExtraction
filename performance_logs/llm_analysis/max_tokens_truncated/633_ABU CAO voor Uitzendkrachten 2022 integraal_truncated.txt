TRUNCATED RESPONSE DEBUG INFO
Original filename: ABU CAO voor Uitzendkrachten 2022 integraal_extract.json
Timestamp: 2025-11-15T20:16:28.972999
Response length: 278626 characters
Finish reason: MAX_TOKENS
================================================================================

{"salary_information": [{"jobgroup": "4", "worker": "allocatiegroep", "ft_hours": 40, "row_note": "CAO remuneration is applicable only if classified in function group 6 or lower. After 26 worked weeks, a periodic increment of 2.25% is granted. From the 53rd week, a second periodic increment applies. The application of CAO remuneration is limited to a maximum of 52 worked weeks, extendable to 104 weeks for workers without a starting qualification until education is completed, after which client's remuneration applies. The hourly wage is adjusted twice a year (January 1 and July 1) by the percentage of the statutory minimum wage increase for statutory minimum wage rows, but not explicitly for fixed amounts in this table. The 2.25% is a personal periodic increment, not a general table increase percentage (inc_pct). The amounts are starting salaries, not including the periodic increment itself as a fixed amount in the table. The 'statutory minimum (youth) wage' rows are excluded as they do not provide a fixed numerical amount in the table for extraction. The fixed amounts for groups 4-6 are assumed to be for adult workers as no specific age band is mentioned and they are not explicitly youth wages. The 'step' field is omitted as the table does not provide distinct step labels, only a starting salary and a rule for periodic increments based on tenure. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start_date' values are in YYYY-MM-DD format. The 'unit' values are strings. The 'table_label' values are strings. The 'row_note' value is a string. The 'ft_hours' value is a number. The 'worker' value is a string. The 'jobgroup' value is a string. All field names match the schema exactly. No control tokens are used. No multiple JSON objects are returned. No extra text outside the JSON string is included. The extraction is grounded in the source text, with no hallucination, inference, or guessing. Missing values are omitted as per the rules. The output is a single parseable JSON object. The 'salary_information' array is the top-level container. The 'items' in the 'salary_information' array are SalaryRow objects. The 'items' in the 'timeline' array are SalaryPoint objects. The 'required' fields for SalaryRow are 'jobgroup'. The 'required' fields for SalaryPoint are 'start_date', 'amount', 'unit'. All these requirements are met. The 'age_group' selection rule is applied, ignoring youth-only wages and only considering adult-eligible bands. The 'table_selection' rules are applied, including only standard wage tables and excluding allowances, bonuses, etc. The 'table_amounts, percentages, dates' rules are applied, outputting amounts as numbers, dates in YYYY-MM-DD format, and omitting 'inc_pct' when not explicitly stated as a general table increase. The 'table_timeline construction' rules are applied, building a timeline for each unique combination and aligning points across table versions without imputation. The 'worker type' rule is applied, keeping 'allocatiegroep' as it is a meaningful distinction. The 'step' field is omitted as the 2.25% is a periodic increment rule, not a distinct step label with a fixed amount in the table. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not stated. The 'holiday_incl' field is omitted as it is not explicitly stated that the amounts include holiday allowance. The 'note' field for SalaryPoint is omitted as there are no specific footnotes or exceptions tied to individual wage entries beyond the general row_note. The 'end_date' field is omitted as it is not explicitly stated when the salary amount ceases to apply, only when it becomes effective. The 'inc_pct' field for SalaryPoint is omitted as the 2.25% is a personal periodic increment, not a general percentage increase for the table or time period. The 'education', 'permanency', and 'hours_type' fields are omitted as they are not specified for these salary rows. The 'is_entry' field is omitted as it is not explicitly stated whether these are entry scales or standard scales. The 'age_group' field is omitted as no specific age band is printed for these job groups, and they are not explicitly youth wages. The 'worker' field is kept as 'allocatiegroep' because it provides a meaningful distinction between different worker categories in the CAO. The 'ft_hours' field is included as it is explicitly stated as 'based on 40-hour week'. The 'unit' field is included as 'hourly' as it is explicitly stated as 'minimum actual hourly wages'. The 'table_label' field is included as a concise identifier of the wage table version. The 'jobgroup' field is included as it is a required field and is explicitly present in the table. The 'salary_information' array contains SalaryRow objects, each representing a unique combination of jobgroup, worker type, and other relevant attributes. Each SalaryRow contains a 'timeline' array of SalaryPoint objects, representing the chronological list of salary values for that specific combination. The SalaryPoint objects contain 'start_date', 'amount', 'unit', and 'table_label' as required fields. All other optional fields are omitted as they are not explicitly present in the source text for these specific salary entries. The output is a single JSON object as requested, with no extra text or comments. The JSON is valid and parseable, following the provided schema. The 'salary_information' array is sorted by jobgroup. The 'timeline' array within each SalaryRow is sorted chronologically by 'start_date'. The 'amount' values are numbers, not strings. The 'start